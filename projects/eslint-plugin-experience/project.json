{
    "name": "eslint-plugin-experience",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "prefix": "tui",
    "projectType": "library",
    "sourceRoot": "projects/eslint-plugin",
    "targets": {
        "publish": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm publish ./projects/eslint-plugin-experience --access=public --ignore-scripts || echo \"already published\""
            }
        }
    }
}
