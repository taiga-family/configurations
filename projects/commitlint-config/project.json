{
    "name": "commitlint-config",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "projects/commitlint-config",
    "projectType": "library",
    "prefix": "tui",
    "targets": {
        "publish": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm publish ./projects/commitlint-config --access=public --ignore-scripts || echo \"already published\""
            }
        }
    }
}
